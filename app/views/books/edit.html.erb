<h1>編集画面</h1>

<%= form_with model: @book do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Upadate Book' %>
  
  <%= link_to "Show", user_path(current_user.id) %><%= link_to "Back", :back %>
  
<% end %>